other
initialize

	super initialize.

	fieldDefinitions := Dictionary new.
	gridDefinitions := Dictionary new.
	isRepeatable := false.
	subSections := OrderedCollection new.
	libraryComponentReferences := OrderedCollection new.