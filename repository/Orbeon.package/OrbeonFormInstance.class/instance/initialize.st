other
initialize

	super initialize.

	draft  := 'N'.
	username := ''.
	groupname := ''.
	fields := Dictionary new.
	fieldsValues := Dictionary new.
	sections := OrderedCollection new.
	isValid := false.